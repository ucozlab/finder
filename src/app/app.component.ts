import { Component } from "@angular/core";

@Component({
    selector: 'finder-app',
    template: `
       <h1>{{title}}</h1>
       <nav>
            
       </nav>
       <main>
            
       </main>
  `
})
export class AppComponent {
    title = 'Finder';
}